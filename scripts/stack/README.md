# Arranque de la pila

**Documentación:** [docs/scripts/stack-arranque-pila.md](../../docs/scripts/stack-arranque-pila.md)

`start_stack.sh`, `stop_stack.sh`, `status_stack.sh`. Orden: HDFS → YARN → Hive → MongoDB → Kafka → NiFi → Airflow.
