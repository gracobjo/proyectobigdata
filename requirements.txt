# Python Dependencies para el Proyecto Big Data

# Apache Spark
pyspark==3.5.0

# Kafka (Python 3.12: PyPI 2.0.2 falla con kafka.vendor.six.moves; usar versión desde GitHub)
git+https://github.com/dpkp/kafka-python.git
# Codec snappy para consumir mensajes comprimidos por Kafka
python-snappy

# GraphFrames: Python bindings (JARs con --packages io.graphframes:graphframes-spark3_2.12:0.10.0)
graphframes-py>=0.10.0
setuptools

# Airflow
apache-airflow==2.10.0
apache-airflow-providers-apache-spark==4.4.0

# MongoDB
pymongo>=4.6.0

# Utilidades
python-dateutil==2.8.2
pytz==2024.1

# Testing (opcional)
pytest==7.4.3
pytest-spark==0.6.0

# Utilidades adicionales
requests==2.31.0
pandas==2.1.4

# Visualización del grafo (Streamlit)
streamlit>=1.28.0
pyvis>=0.3.2
networkx>=3.0

# API REST (Swagger / OpenAPI)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# IA / recomendación de rutas (Paso 2: entrenar modelo)
scikit-learn>=1.3.0
joblib>=1.3.0
