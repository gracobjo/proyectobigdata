# Copiar a .env y ajustar si es necesario
# cp .env.example .env

# Kafka: desde el HOST usar localhost:9092; desde contenedores usan kafka:9092
# Si ejecutas scripts desde el host y fallan, añade en /etc/hosts: 127.0.0.1 kafka

# API (solo si sobreescribes en compose)
# MONGO_URI=mongodb://mongodb:27017/
# DB_NAME=transport_db

# Para despliegue en Railway: definir variables en el panel de Railway
# (MONGO_URI apuntando al servicio MongoDB de Railway, etc.)
