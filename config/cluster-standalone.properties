# Configuración para modo STANDALONE (un solo nodo, todo en localhost)
# Usar este archivo o exportar estas variables cuando se trabaje sin nodo2.
# Para scripts que lean variables: source config/cluster-standalone.properties (bash)
# En los scripts Python del proyecto ya se usa localhost directamente.

HDFS_NAMENODE=localhost
HDFS_PORT=9000
KAFKA_BROKER=localhost
KAFKA_PORT=9092
HDFS_URL=hdfs://localhost:9000
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
