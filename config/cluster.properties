# Configuración del Cluster Distribuido
# Este archivo centraliza las configuraciones de red del cluster

# Nodos del cluster
NAMENODE_HOST=nodo1
DATANODE_HOSTS=nodo1,nodo2
RESOURCEMANAGER_HOST=nodo1
NODEMANAGER_HOSTS=nodo1,nodo2

# HDFS Configuration
HDFS_NAMENODE=nodo1
HDFS_PORT=9000
HDFS_NAMENODE_HTTP_PORT=9870
HDFS_DATANODE_PORT=9864

# YARN Configuration
YARN_RESOURCEMANAGER=nodo1
YARN_RESOURCEMANAGER_PORT=8088
YARN_NODEMANAGER_PORT=8042

# Kafka Configuration
KAFKA_BROKER=nodo1
KAFKA_PORT=9092
KAFKA_CONTROLLER_PORT=9093

# MongoDB Configuration
MONGODB_HOST=nodo1
MONGODB_PORT=27017
MONGODB_DATABASE=transport_db

# Hive Configuration
HIVE_METASTORE_HOST=nodo1
HIVE_METASTORE_PORT=9083

# NiFi Configuration
NIFI_HOST=nodo1
NIFI_PORT=8443

# Airflow Configuration
AIRFLOW_HOST=nodo1
AIRFLOW_PORT=8080

# URLs completas
HDFS_URL=hdfs://${HDFS_NAMENODE}:${HDFS_PORT}
KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BROKER}:${KAFKA_PORT}
MONGODB_URI=mongodb://${MONGODB_HOST}:${MONGODB_PORT}/${MONGODB_DATABASE}
HIVE_METASTORE_URI=thrift://${HIVE_METASTORE_HOST}:${HIVE_METASTORE_PORT}
