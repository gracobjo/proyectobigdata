# Archive

Los flujos que no importaban en NiFi 2.7 (`transport_monitoring_flow.json`, `iot_sensors_flow.json`) se eliminaron. Esta carpeta se mantiene por si en el futuro se archivan otros ficheros.

Para importar en NiFi 2.7 usa solo los `*_importable.json` de la raíz de `ingestion/nifi/`.
