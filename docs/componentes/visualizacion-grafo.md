# Visualización del grafo de red

App Streamlit con grafo interactivo (Pyvis) y bottlenecks desde MongoDB. Ejecutar: `streamlit run viz/app_grafo.py` (desde la raíz, venv activado). Navegador: http://localhost:8501. Requisitos: `pip install -r requirements.txt` (streamlit, pyvis, networkx).
